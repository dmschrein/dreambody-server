{
  "name": "dreambody-lambda-resolvers",
  "version": "1.0.0",
  "description": "Lambda resolvers for DreamBody GraphQL API",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w"
  },
  "dependencies": {
    "@aws-sdk/client-bedrock-runtime": "^3.856.0",
    "@aws-sdk/client-dynamodb": "^3.427.0",
    "@aws-sdk/lib-dynamodb": "^3.427.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.124",
    "@types/node": "^20.8.6",
    "@types/uuid": "^9.0.7",
    "typescript": "^5.2.2"
  }
}
